# 2026년 8월 24일 Today I Learned

## Python
  1. set을 쓰지말고 list 기능만으로 unique_tag를 구현
     
    tags = ["Python","AI","Python","Data","AI"]

    unique_tags =[]
    for tag in tags:
        """ Way1
        isTag = False
        for i in range(len(unique_tags)):
            if unique_tags[i] == tag:
                isTag = True
                break;
        if not isTag: # if isTag == False:
            unique_tags.append(tag)
        """
    
        # Way2
        if tag not in unique_tags:
            unique_tags.append(tag)
    
    print(unique_tags)

  - Way 1
    1. 비어있는 unique_tags list 생성, 이 list에 unique한 값을 담을 예정
    2. tags 전체에 대해 각각의 tag가 unique_tags에 있는지 여부를 for loop를 통해 확인
    3. isTag 변수(현재 tag가 포함되어 있는지 여부 확인)를 생성하고, 초기값을 False(포함되어 있지 않음)로 지정
    4. 현재 tag가 unique_tags에 포함되었는지를 for loop로 확인
    5. 포함되어 있으면, isTag를 True(포함되어 있음)으로 변경 후 for loop 종료
    6. for loop 종료 후, isTag가 False(포함되어 있지 않음)인 경우 unique_tags에 append 함
       
       이때 비교 연산자 "Tag == False"는 "not isTag" 로 표현 가능
  - Way 2
    
    4. 현재 tag가 unique_tags에 포함되었는지 여부를 not in List를 통해 확인
    5. 포함되어 있지 않는 경우 append 함

  - Way 2의 방식이 보다 Python의 장점을 살린 코드라 생각

    
## My_project

  - Project Name: Ax-News
  - - ax-news
    -   기능: 메세지 전송
    - ax-news-admin
    -   기능: RSS 연결하여 관련 내용 크롤링
